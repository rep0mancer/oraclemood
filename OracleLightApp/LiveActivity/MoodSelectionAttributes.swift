//
// NOTE: MoodSelectionAttributes is implemented in the OracleLightWidget target.
// This stub is disabled to prevent duplicate definitions when compiling the
// application. See the widget target for the actual ActivityAttributes.
//
#if false
import Foundation
import ActivityKit

/// Defines the attributes for the mood selection Live Activity. There is no
/// dynamic content state since the activity does not update externally.
struct MoodSelectionAttributes: ActivityAttributes {
    public typealias ContentState = Void
    // No dynamic properties needed
}
#endif